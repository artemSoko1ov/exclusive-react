import './Logo.scss'

const Logo = (props) => {
  const {} = props
  return (
    <div className="logo">
      <span
        className="logo__text"
      >
        Exclusive
      </span>
    </div>
  )
}

export default Logo